name: Email Action

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2001691@ds.study.iitm.ac.in
        run: echo "Hello, world!"
